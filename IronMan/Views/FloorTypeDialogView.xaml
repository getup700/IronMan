<Window x:Class="IronMan.Revit.Views.FloorTypeDialogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IronMan.Revit.Views"
        mc:Ignorable="d"
        Title="FloorTypeDialogView" Height="300" Width="200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="50" />
            <RowDefinition Height="50" />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>
        <DockPanel VerticalAlignment="Center">
            <TextBlock Text="偏移量：" />
            <TextBlock Text="{Binding Offset}" Width="100" DockPanel.Dock="Right"/>
        </DockPanel>
        <Button Grid.Row="3"
                Content="确定"
                Command="{Binding SubmitCommand}" />
    </Grid>
</Window>
